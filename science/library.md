There are some libraries that can be used such as pytorch and tensorflow.
However, because the most advance library to work with remote sensing is torchgeo then we're going to start learning to use this [[torchgeo]] library with [[pytorch]] act as a pre-requisite.
